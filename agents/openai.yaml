interface:
  display_name: "Codex Managed Worktree Threads"
  short_description: "Explicit-only Codex App worktree threads"
  default_prompt: "Use explicit-only $codex-managed-worktree-threads to run one Codex App background thread per task with Codex-managed worktrees."
policy:
  allow_implicit_invocation: false
